---
description: Component placement and folder structure for app segments
globs: app/**/*.tsx, app/**/*.ts
---

# Component Structure

## Placement by scope

- **Page-private components**: Put in a `components/` folder **at that segment level** (e.g. `app/exchange-rate/components/`). Only that page/segment imports them.
- **Shared by children only**: Put in the **parent segment’s** `components/` (e.g. shared by `app/fuel-price/api` and `app/fuel-price/mcp` → `app/fuel-price/components/`).
- **Global / app-wide**: Keep at the **root** `components/` (project root), e.g. `JsonViewer`, `Alert`, `Spinner`.

## Folder and entry

- Use a **single entry** for each `components/` folder: `index.ts` (or `index.tsx`) that re-exports all public components.
- Pages and siblings import from that folder: `import { Foo } from './components'` or `import { Foo } from '@/app/.../components'`.
- If a **single file is too large or holds multiple components**, split into a **folder** (e.g. `components/Foo/`) and expose one entry (e.g. `components/Foo/index.ts`) so callers still use one import path.

## Examples

- `app/login/components/Form.tsx` + `app/login/components/index.ts` → `app/login/page.tsx` uses `import { LoginForm } from './components'`.
- `app/exchange-rate/api/components/ExchangeRateApiPlayground.tsx` + `index.ts` → api page imports from `./components`.
- Root-level `components/JsonViewer.tsx` → any page can `import ... from '@/components/JsonViewer'`.
