import { ManagePlaceholderSkeleton } from '@/components/manage'

/**
 * Instant shell while manage page validates session and renders.
 * @returns Placeholder manage skeleton
 */
export default function ManageLoading() {
  return <ManagePlaceholderSkeleton />
}
