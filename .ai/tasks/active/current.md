# Active tasks

Tasks currently in progress. When a task is done, move it to `tasks/done/` or record it in `tasks/done/README.md`.

**Current:** No active tasks. Recent completions: T-001, T-002, T-003, T-DNS — see `tasks/done/README.md`.

---

## Template for new tasks

```markdown
### T-XXX: Short title

- **Spec / Plan:** link to doc(s).
- **Done when:** concrete, checkable criteria.
```
