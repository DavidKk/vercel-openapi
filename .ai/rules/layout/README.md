# Layout rules

Module shell, sidebar, page structure, component placement. Load when working on UI layout or new module pages.

---

| File                       | Purpose                                                                                      |
| -------------------------- | -------------------------------------------------------------------------------------------- |
| **module-layout.md**       | Global shell, module layout, 5 sidebar entries, API/MCP doc+playground, API handler pattern. |
| **component-structure.md** | Where to put components (page vs shared vs root), `index.ts` re-exports.                     |

Generator uses `.ai/rules/module-layout.yaml` (in rules root); for human/AI context read the `.md` files here.
