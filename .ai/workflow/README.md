# Workflow

Process definitions for AI and humans. **Read the relevant workflow before starting the task.**

| Workflow               | When to use                                                                                                           |
| ---------------------- | --------------------------------------------------------------------------------------------------------------------- |
| **Module development** | Adding or generating a new feature module. Read `workflow/module-development.md` and follow the five phases in order. |

New workflows (e.g. API change, release) can be added here and under `workflow/<name>.md`.
