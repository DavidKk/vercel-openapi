import { createLogger } from '@/services/logger'

/** Module logger for china-geo service; use this in index and any submodules to avoid circular imports */
export const logger = createLogger('china-geo')
